using namespace std ;
#include "iostream"
#include "stdio.h"
#include "algorithm"
#include "cstring"
#include "cmath"
#include "set"
#include "vector"
#include "iomanip"
#include "stack"
#include "cctype"
typedef long long lld;
typedef vector<int> vi;
typedef pair<int,int> pii;
typedef vector<pii> vpii;
#define _CRT_SECURE_NO_DEPRECATE
#define MAX 200
#define CFT int ACCEPTED = 1 ;
#define MYT int ACCEPTED = 1000 ;
#define T_ISA ACCEPTED--
#define AlhamdwLeAllah return 0
#define el '\n'
#define watch(x) cout << (#x) << " is " << (x) << endl
void SpeedIO()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
}
void write_to_file()
{
    freopen("output.txt","w",stdout);
}
bool isVowel(char c)
{
    if(c=='a' || c=='e' || c=='i' || c=='o' || c=='u')
        return true ;
    else
        return false ;
}
int SD()
{
    int x;
    scanf("%d",&x);
    return x;
}
lld SLL()
{
    lld x;
    scanf("%lld",&x);
    return x;
}
char SC()
{
    char x;
    scanf("%c",&x);
    return x;
}
const int AKA = 6e5 + 5 ;
int idx [AKA], a [AKA], b [AKA], arr[AKA], fr[AKA], cm [AKA];
bool cpm (int s, int f)
{
    if(b [f] < b[s])
        return true;
    if(b[f] == b[s] && a[f] < a[s])
        return true;
    return false;
}
char s[101];
int main()
{
   // write_to_file();
    CFT;
    while(T_ISA)
    {
        int t=SD();
        for(int n=1; n<=t; ++n)
        {
            cin>>s;
            int Size=strlen(s);
            char c=s[0] ;
            int l=0;
            printf("Case %d: ",n);
            for(int i=1; i<Size; ++i)
            {
                if(!isalpha(s[i]))
                    l=l*10+(s[i]-'0');
                if(isalpha(s[i]) || i==Size-1)
                {
                    for(int j=0; j<l; ++j)
                        printf("%c",c);
                    c=s[i];
                    l=0;
                }
            }
            printf("\n");
        }
    }
    AlhamdwLeAllah;
}
/*



                                ????????????????????????????????????
                                ?????????????????????????????????????
                                ?????????????????????????????????????
                                ?????????????????????????????????????
                                ?????????????????????????????????????
                                ?????????????????????????????????????
                                ?????????????????????????????????????
                                ?????????????????????????????????????
                                ????????????????????????????????????
                                ???????????????????????????????????

?????????????????? > Made By Screaming Eagle AhMeD SaLaH  <??????????????????>
*/