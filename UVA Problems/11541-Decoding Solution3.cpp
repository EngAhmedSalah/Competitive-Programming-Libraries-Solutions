using namespace std ;
#include "iostream"
#include "stdio.h"
#include "algorithm"
#include "cstring"
#include "cmath"
#include "set"
#include "vector"
#include "iomanip"
#include "stack"
#include "cctype"
typedef long long lld;
typedef vector<int> vi;
typedef pair<int,int> pii;
typedef vector<pii> vpii;
#define _CRT_SECURE_NO_DEPRECATE
#define MAX 200
#define CFT int ACCEPTED = 1 ;
#define MYT int ACCEPTED = 1000 ;
#define T_ISA ACCEPTED--
#define AlhamdwLeAllah return 0
#define el '\n'
#define watch(x) cout << (#x) << " is " << (x) << endl
void SpeedIO()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
}
void write_to_file()
{
    freopen("output.txt","w",stdout);
}
bool isVowel(char c)
{
    if(c=='a' || c=='e' || c=='i' || c=='o' || c=='u')
        return true ;
    else
        return false ;
}
int SD()
{
    int x;
    scanf("%d",&x);
    return x;
}
lld SLL()
{
    lld x;
    scanf("%lld",&x);
    return x;
}
char SC()
{
    char x;
    scanf("%c",&x);
    return x;
}
const int AKA = 6e5 + 5 ;
int idx [AKA], a [AKA], b [AKA], arr[AKA], fr[AKA], cm [AKA];
bool cpm (int s, int f)
{
    if(b [f] < b[s])
        return true;
    if(b[f] == b[s] && a[f] < a[s])
        return true;
    return false;
}
int main()
{
    // write_to_file();
    CFT;
    while(T_ISA)
    {
        int t=SD();
        for(int i=1; i<=t; ++i)
        {
            int arr[1000]={0};
            char s[101];
            char word[101];
            cin>>word;
            int Size=strlen(word);
            for(int i=0,n=0,c=0; i<Size; ++i)
            {
                if(isalpha(word[i]))
                    s[c++]=word[i];
                else if(!isalpha(word[i]) && isalpha(word[i+1]))
                {
                    arr[n]=arr[n]*10+(word[i]-'0');
                    ++n;
                }
                else
                    arr[n]=arr[n]*10+(word[i]-'0');
            }
            printf("Case %d: ",i);
            for(int i=0; arr[i]; ++i)
            {
                for(int j=0; j<arr[i]; ++j)
                    printf("%c",s[i]);
            }
            printf("\n");
        }
    }
    AlhamdwLeAllah;
}
/*



                                ????????????????????????????????????
                                ?????????????????????????????????????
                                ?????????????????????????????????????
                                ?????????????????????????????????????
                                ?????????????????????????????????????
                                ?????????????????????????????????????
                                ?????????????????????????????????????
                                ?????????????????????????????????????
                                ????????????????????????????????????
                                ???????????????????????????????????

?????????????????? > Made By Screaming Eagle AhMeD SaLaH  <??????????????????>
*/